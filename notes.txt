To run VS Code from Ubuntu simply go into the repo and type 'code .'

to initialize the server, go into the Project folder and type 'rails server'

to create a new controller, use 'rails generate controller Welcome index' 
    Welcome is the directory and index will be the html page

CRUD (Create, Read, Update, Destroy) operations

Add requirements and global variables into the config/initialize folder